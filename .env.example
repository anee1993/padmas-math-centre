# Database Configuration
DATABASE_URL=jdbc:postgresql://db.bvtawdcbfkwbklhhovre.supabase.co:5432/postgres
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-database-password-here

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-minimum-256-bits-change-this-in-production
JWT_EXPIRATION=86400000

# Supabase Configuration
# CRITICAL: Use service_role key (NOT anon key) for backend operations
# Get it from: Supabase Dashboard → Settings → API → service_role key
# The service_role key bypasses Row Level Security (RLS) policies
SUPABASE_URL=https://bvtawdcbfkwbklhhovre.supabase.co
SUPABASE_KEY=your-supabase-service-role-key-here

# Supabase JWT Secret (JWK format for ECC P-256 keys)
# Get this from: Supabase Dashboard > Settings > API > JWT Settings > JWT Secret
# This should be the FULL JWK JSON object for ES256 algorithm
# Example format: {"x":"...","y":"...","alg":"ES256","crv":"P-256","ext":true,"kid":"...","kty":"EC","key_ops":["verify"]}
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-jwk-here

# Email Configuration
MAIL_ENABLED=false
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_FROM=noreply@padmasmathcentre.com
MAIL_FROM_NAME=Padma's Math Centre

# Groq AI Configuration (for Assignment Generator)
# Get your free API key from: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.3-70b-versatile

# Teacher Account Configuration
# Note: Password is only used when creating a NEW teacher account
# Existing teachers can reset password using the forgot password feature
TEACHER_EMAIL=teacher@example.com
TEACHER_DEFAULT_PASSWORD=Teacher@123
TEACHER_NAME=Teacher Name

# Server Configuration
SERVER_PORT=8080
SHOW_SQL=false
